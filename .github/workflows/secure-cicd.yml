name: Secure CI/CD Pipeline

on:
  push:
    branches:
      - main
      - develop
      - 'feature/**'
      - 'release/**'
      - 'hotfix/**'
  pull_request:
    branches:
      - main
      - develop

jobs:
  # ===================================
  # BUILD STAGE
  # ===================================
  build:
    name: Build Stage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22 # Updated to match Ghost requirement

      - name: Install PNPM Globally
        run: npm install -g pnpm

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Ghost Production
        # FIX: Increased memory to 4GB to prevent Exit Code 130 crash
        env:
          NODE_OPTIONS: "--max-old-space-size=4096"
        run: pnpm build

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: |
            dist
            apps
            ghost

  # ===================================
  # TEST STAGE
  # ===================================
  test:
    name: Test Stage
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install PNPM Globally
        run: npm install -g pnpm

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Test Validation
        run: echo "Unit tests skipped for DevSecOps project demo"

  # ===================================
  # SECURITY SCAN + DOCKER BUILD
  # ===================================
  security-scan:
    name: Security Scan
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # SNYK SCAN (Added for full DevSecOps compliance)
      - name: Run Snyk Security Scan
        uses: snyk/actions/node@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --severity-threshold=high

      - name: Build Docker Image
        run: |
          docker build \
          -t ${{ secrets.DOCKER_USERNAME }}/ghost-app:latest \
          -f Dockerfile.production .

      - name: Trivy Scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '${{ secrets.DOCKER_USERNAME }}/ghost-app:latest'
          format: 'table'
          exit-code: '0'

      - name: Push Docker Image
        if: github.event_name == 'push'
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/ghost-app:latest

  # ===================================
  # DEPLOY STAGES
  # ===================================
  deploy-staging:
    name: Deploy to Staging
    needs: security-scan
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop'
    steps:
      - name: Deploy to Staging
        run: echo "Staging deployment successful"

  deploy-production:
    name: Deploy to Production
    needs: security-scan
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    environment:
      name: production
      url: https://ghost.yourdomain.com
    steps:
      - name: Deploy to Production
        run: echo "Production deployment successful"

  # ===================================
  # NOTIFICATIONS
  # ===================================
  notify-success:
    name: Success Notification
    needs: [deploy-production, deploy-staging]
    runs-on: ubuntu-latest
    if: success()
    steps:
      - name: Slack Notification
        run: |
          curl -X POST -H "Content-type: application/json" \
          --data '{"text":"✅ DevSecOps Pipeline SUCCESS - Ghost deployed"}' \
          ${{ secrets.SLACK_WEBHOOK }}

  notify-failure:
    name: Failure Notification
    runs-on: ubuntu-latest
    if: failure()
    steps:
      - name: Slack Notification
        run: |
          curl -X POST -H "Content-type: application/json" \
          --data '{"text":"❌ DevSecOps Pipeline FAILED - Check logs"}' \
          ${{ secrets.SLACK_WEBHOOK }}