name: Secure CI/CD Pipeline

on:
  push:
    branches:
      - main
      - develop
      - 'feature/**'
      - 'release/**'
      - 'hotfix/**'

  pull_request:
    branches:
      - main
      - develop

jobs:

  # ===================================
  # BUILD STAGE
  # ===================================
  build:
    name: Build Stage
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # Setup Node.js first
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      # Install PNPM globally (most reliable)
      - name: Install PNPM Globally
        run: npm install -g pnpm

      # Verify tools
      - name: Verify Environment
        run: |
          node --version
          npm --version
          pnpm --version

      # Install dependencies
      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      # Build only Ghost production (not full monorepo)
      - name: Build Ghost Production
        run: pnpm build:production

      # Upload build artifacts
      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: |
            ghost/core
            apps/admin/dist

  # ===================================
  # TEST STAGE
  # ===================================
  test:
    name: Test Stage
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install PNPM Globally
        run: npm install -g pnpm

      - name: Verify PNPM
        run: pnpm --version

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      # Safer test command for Ghost
      - name: Run Unit Tests
        run: pnpm nx run ghost:test:unit

  # ===================================
  # SECURITY SCAN STAGE
  # ===================================
  security-scan:
    name: Security Scan
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install PNPM Globally
        run: npm install -g pnpm

      - name: Verify PNPM
        run: pnpm --version

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      # -------------------------------
      # SNYK DEPENDENCY SCAN
      # -------------------------------
      - name: Run Snyk Security Scan
        uses: snyk/actions/node@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --severity-threshold=high

      # -------------------------------
      # BUILD DOCKER IMAGE
      # -------------------------------
      - name: Build Docker Image
        run: docker build -t ghost:${{ github.sha }} .

      # -------------------------------
      # TRIVY IMAGE SCAN
      # -------------------------------
      - name: Run Trivy Vulnerability Scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ghost:${{ github.sha }}
          format: sarif
          output: trivy-results.sarif

      - name: Upload Trivy Results
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: trivy-results.sarif

  # ===================================
  # DOCKER BUILD + PUSH
  # ===================================
  docker-build:
    name: Docker Build and Push
    needs: security-scan
    runs-on: ubuntu-latest
    if: github.event_name == 'push'

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/ghost:latest
            ${{ secrets.DOCKER_USERNAME }}/ghost:${{ github.sha }}

  # ===================================
  # DEPLOY TO STAGING
  # ===================================
  deploy-staging:
    name: Deploy to Staging
    needs: docker-build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop'

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Deploy to Staging
        run: |
          echo "Deploying Ghost to staging environment..."
          # Example:
          # kubectl apply -f kubernetes/staging/

  # ===================================
  # DEPLOY TO PRODUCTION
  # ===================================
  deploy-production:
    name: Deploy to Production
    needs: docker-build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    environment:
      name: production
      url: https://ghost.yourdomain.com

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Deploy to Production
        run: |
          echo "Deploying Ghost to production with zero downtime..."
          # Example:
          # kubectl apply -f kubernetes/production/

      - name: Smoke Test
        run: |
          echo "Running smoke tests..."
          echo "Production deployment verified successfully"
